/**
 * Module-wide constants. Safe to import from anywhere (no server-only deps).
 *
 * Wirer placement: <output>/frontend/src/lib/auth/constants.ts
 */
export const SESSION_COOKIE_NAME = 'spike_session'
export const SESSION_COOKIE_MAX_AGE_SECONDS = 60 * 60 * 24 * 7
