id: CtaFullBleed
displayName: "Full-bleed CTA section"
description: "Edge-to-edge CTA with image background or gradient fallback. Big-finale conversion block."
version: 1.0.0

category: cta
density: spacious
responsive: mobile-first

componentFile: ./CtaFullBleed.tsx

props:
  bgImageUrl:
    type: image
    label: "Background image"
  eyebrow:
    type: string
    label: "Eyebrow kicker"
  headline:
    type: string
    label: "Headline"
    required: true
  body:
    type: string
    label: "Body"
  ctaLabel:
    type: string
    label: "CTA label"
    required: true
  ctaHref:
    type: string
    label: "CTA URL"
    required: true

tags: [cta, full-bleed, conversion]
bestWithThemes: [sunset, midnight, aurora]
deprecated: false
