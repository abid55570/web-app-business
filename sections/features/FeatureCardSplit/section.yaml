id: FeatureCardSplit
displayName: "Feature card — image/text split rows"
description: "Stacked feature cards, each row a 50/50 image+text split. Even rows mirror the image to the right."
version: 1.0.0

category: features
density: comfortable
responsive: mobile-first

componentFile: ./FeatureCardSplit.tsx

props:
  heading:
    type: string
    label: "Section heading"
  items:
    type: array
    label: "Feature rows"
    required: true
    description: "{ title, body, bullets?, imageUrl }"

tags: [features, split, marketing]
bestWithThemes: [glass, soft, editorial]
deprecated: false
