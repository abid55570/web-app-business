id: Callout
displayName: "Inline callout box (admonition)"
description: "Markdown-style admonition box — note / tip / warning / danger, with icon + optional title."
version: 1.0.0

category: content
density: comfortable
responsive: mobile-first

componentFile: ./Callout.tsx

props:
  variant:
    type: enum
    label: "Callout variant"
    enum: [note, tip, warning, danger]
    default: note
  title:
    type: string
    label: "Title"

tags: [content, admonition, callout]
bestWithThemes: [editorial, minimal, mono]
deprecated: false
